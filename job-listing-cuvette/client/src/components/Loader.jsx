import React from 'react';
import '../Loader.css';

const Loader = () => {
    return (
        <div className="loading-overlay">
            <div className="loader"></div>
        </div>
    );
};

export default Loader;